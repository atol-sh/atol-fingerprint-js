<!-- Thanks for contributing to @atol-sh/fingerprint. -->

## What does this change?

<!-- A short description of the user-facing effect. -->

## Why?

<!-- Context / linked issue. -->

Closes #

## Privacy / signal implications

<!-- Note any change to the collected signal set, the consent/opt-out paths
(disabled, GPC), the submit contract, or anything that affects what leaves the
browser. -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes (tests added/updated for behavior changes)
- [ ] `npm run build` succeeds
- [ ] No new production dependencies (or justified in the description)
- [ ] Docs / CHANGELOG updated if the public API or signal set changed
